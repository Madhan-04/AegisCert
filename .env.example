# AegisCert Environment Configuration Example

# URL of the backend API (use Render backend URL when deploying to production)
VITE_API_URL=http://localhost:5000

# Optional Supabase integration keys (if using Supabase instead of SQLite backend)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
